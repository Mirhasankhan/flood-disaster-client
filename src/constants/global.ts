export const categoryNames = ["All", "Hygine", "Food", "Dress"];

export const categories = categoryNames.map((item) => ({
  value: item,
  label: item,
}));
export const roleNames = ["recipient", "donor"];

export const roles = roleNames.map((item) => ({
  value: item,
  label: item,
}));
export const ratingsList = ["1", "2", "3", "4", "5"];

export const ratings = ratingsList.map((item) => ({
  value: item,
  label: item,
}));
